generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Tarefas {
  id        Int    @id @default(autoincrement())
  nome      String
  descricao String
  inicio    String
  fim       String
  imagem    String
}
